{
  "name": "ats-resume-checker",
  "version": "1.0.0",
  "description": "AI-powered ATS resume checker and optimizer",
  "private": true,
  "scripts": {
    "build": "cd function && npm install && npm run build",
    "build:all": "npm install && cd function && npm install && npm run build",
    "dev": "cd function && node server.js",
    "start": "cd function && node server.js",
    "build:function": "cd function && npm install && npm run build",
    "type-check": "tsc --noEmit -p ."
  },
  "keywords": [
    "resume",
    "ats",
    "ai",
    "optimizer"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.1.3",
    "@vercel/node": "^3.0.0",
    "axios": "^1.6.0",
    "mammoth": "^1.6.0",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "@types/pdf-parse": "^1.1.4",
    "@vercel/node": "^5.6.3",
    "typescript": "^5.9.3"
  }
}
